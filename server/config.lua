GlobalConfig = {
    UseDatabase = true,
    DatabaseType = "File", -- "File" or "MySQL"
    AutosaveTime = 60, -- Automatic save interval in seconds, Only for "File" database
    AnounceSaveConsole = true, -- Anounce the automatic save in the console?
}